version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: optiload
      POSTGRES_USER: optiload
      POSTGRES_PASSWORD: optiload
    ports:
      - '5432:5432'

  backend:
    build: ./backend
    ports:
      - '8000:8000'
    environment:
      DATABASE_URL: postgresql://optiload:optiload@postgres:5432/optiload
      SECRET_KEY: dev-secret
    depends_on:
      - postgres

  frontend:
    build: ./frontend
    ports:
      - '3000:3000'
    environment:
      VITE_API_URL: http://localhost:8000
    depends_on:
      - backend
